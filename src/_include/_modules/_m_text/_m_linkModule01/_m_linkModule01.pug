mixin m-linkModule01(tg)
	-
		var op = {
			url:       tg.url ? tg.url : "",
			blank:     tg.blank ? tg.blank : "",
		}
	if(op.blank=="_blank")
		a(href= op.url target="_blank").m-linkModule01
			span
				block
	else
		a(href= op.url).m-linkModule01
			span
				block